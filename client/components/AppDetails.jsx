import React from 'react';


const AppDetails = props => {
  const { app } = props;
  return (
    <div className="app-details">
      {/* <h1>{app.name}</h1>
      <p>{app.description}</p>
      <p>{app.category}</p>
      <p>{app.price}</p> */}
      hi
    </div>
  );
};

export default AppDetails;